[workspace]
members = ["tests", "contracts/smt_contracts", "contracts/cycles_lib", "contracts/cycles_rust", "contracts/cycles_c"]

[profile.release]
overflow-checks = true
opt-level = 's'
lto = true
codegen-units = 1
panic = 'abort'
